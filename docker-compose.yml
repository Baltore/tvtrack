services:
  mongo:
    image: mongo:7
    container_name: tvtrack-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - tvtrack-mongo-data:/data/db

volumes:
  tvtrack-mongo-data:
